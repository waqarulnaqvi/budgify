## ================================
## Gradle Settings for Flutter + Firebase
## ================================
#
## JVM settings for Gradle (increase heap & metaspace size)
#org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
#
## Enable AndroidX and Jetifier (needed for Firebase & modern libs)
#android.useAndroidX=true
#android.enableJetifier=true
#
## Kotlin settings
#kotlin.code.style=official
## Disable incremental compilation (fixes cache corruption issues)
#kotlin.incremental=false
#
## Performance optimizations
#org.gradle.daemon=true
#org.gradle.parallel=true
#org.gradle.configureondemand=true
#
## Use R8 (code shrinker) instead of ProGuard
#android.enableR8=true


# =========================================
# Gradle Settings for Flutter + Firebase
# Compatible with Android Gradle Plugin 8.x
# =========================================

# ---- JVM / Performance Settings ----
org.gradle.jvmargs=-Xmx4G -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.configureondemand=true
org.gradle.caching=true

# ---- AndroidX ----
android.useAndroidX=true
# Jetifier required for older libraries
android.enableJetifier=true

# ---- Kotlin ----
kotlin.code.style=official
kotlin.incremental=false

# ---- Dexing ----
android.useFullClasspathForDexingTransform=true

# ---- DO NOT USE ----
# android.enableDexingArtifactTransform=false
# android.enableR8=false
# android.enableD8=false
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
